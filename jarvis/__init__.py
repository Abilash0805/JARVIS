"""JARVIS — a multi-provider AI assistant that can control your PC.

The package is import-safe even when optional dependencies (pyautogui,
playwright, etc.) are missing, so the API/core layers can be used headless.
"""

__version__ = "0.1.0"
